function say(t)
{
	if(typeof(t) !== 'undefined')
	{	
		alert(t);
	}
	else
	{
		alert("undefined'n'stuff");
	}
}




// maybe provide a function to access configuration data such as
// localhost port #
function getConfigData(keys)
{
	var values = {};
	return values;
}
